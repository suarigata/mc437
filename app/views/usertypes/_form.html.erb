<%= form_for(@usertype) do |f| %>
  <% if @usertype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usertype.errors.count, "error") %> prohibited this usertype from being saved:</h2>

      <ul>
      <% @usertype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
